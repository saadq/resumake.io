/**
 * @flow
 */

// Sections must be lowercased due to them being used directly for routes
type Section =
  | 'templates'
  | 'profile'
  | 'education'
  | 'work'
  | 'skills'
  | 'projects'
  | 'awards'
  | 'preview'

export type { Section }
