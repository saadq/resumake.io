/**
 * @flow
 */

const colors = {
  primary: '#fff',
  accent: '#111',
  borders: '#222'
}

const sizes = {
  header: '15vh',
  sideNav: '20vw'
}

const styles = {
  gradient: `linear-gradient(to right, #050505, #080808, #050505)`
}

export { colors, sizes, styles }
